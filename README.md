# ECHO SHIFT · 错位回声

一款**时间错位**网格解谜游戏。没有随机，没有手速，全部确定——只有两条规则，外加两个专门用来**掰弯**这两条规则的东西。

**▶ [在线试玩 · Play now](https://echo-shift-chi.vercel.app)**

![screenshot](docs/screenshot.png)

## 两条规则

> **1. 你走的每一步，会在 N 回合后由一个「回声」原样重演。**
> 回声是实体：它挡路，它踩得住压力板，它撞上你就是时间悖论。
>
> **2. 你每回合都必须移动。没有"原地等待"。**

第二条才是难点所在。因为不能停，**没有人能连续两回合踩住同一块压力板**——
门不会保持开启，它只会**闪一下**：

> **一扇门每被"踩亮"一次，只开一个回合。你必须刚好在那一回合站在门口。**

而且时间也没法白白消耗：想拖延，唯一的办法是**走出去再走回来**，
而你走的这段冤枉路，N 回合后回声会一模一样再走一遍。

### 由此产生的两条硬约束

**奇偶律。** 每走一步，`(x + y) mod 2` 必然翻转。所以你在第 t 回合的位置奇偶性是**锁死的**。
推论一：从压力板到门口的**距离，必须和延迟同奇偶**——差 2 就得绕 2 步，差 1 则**永远无解**。
推论二：延迟为偶数时，回声只可能**踩进你的格子**；延迟为奇数时，它只可能**和你对穿**。
一关会遇到哪种悖论，在它选定延迟的那一刻就决定了。

**回访律。** N 道门串联，就需要 N 次闪门，也就意味着你必须**回到那块板 N 次**，
而且每两次之间的间隔，要正好等于两道门之间的距离。
最后一关 THREE DOORS 就是这样：开局要先在板上来回踩满四次，才能迈出走向第一道门的第一步。

## 两个例外：碎片与哨兵

前十三关只有四种元素。第 14 关起加进来两样，每一样都只为了打破上面某一条约束：

**碎片 `*`（寻宝）。** 出口在你集齐**全部**碎片之前是**封死的**——图上会画一道横杠，
踩上去也不算通关。于是路线不再是「怎么走到门口」，而是「怎么**先绕遍全场**再走到门口」。
回声会直接踩过碎片而不捡起来：**只有「现在的你」能拿东西**。

**哨兵 `S` 与挥砍（攻击）。** 哨兵占住它那一格，走不过去，必须砍掉。
而挥砍是**全局唯一一个不改变你位置的回合**——它就是「不许原地不动」那条规则上唯一的裂缝，
所以它同时做到两件别的动作永远做不到的事：

> **① 翻转奇偶。** 你花掉一回合，却没有改变 `(x + y) mod 2`。
> 第 17 关 OFF BEAT 整关没有任何东西挡路，板到门的距离就是**差一步**——
> 按旧规则这叫「永远无解」，现在你砍一刀就补上了。
>
> **② 让回声停一拍。** 回声重演的是你的**位移**，不是你的动作：
> 它重演那个挥砍回合时，就只是**站着不动**。
> 所以——**站在压力板上挥砍**，N 回合后那个回声会**连续两回合踩住这块板**，
> 门于是**真的开了两个回合**，而不是闪一下。第 18 关 HELD OPEN 的双联门只能这么过。

两条附带规则，会直接影响关卡怎么布：

- **一刀清一圈。** 挥砍会砍掉你**四周所有**哨兵，所以同一格最多只值**一次**暂停，
  在旁边堆再多哨兵也没用。
- **一刀，每个回声各停一拍。** 一次挥砍会被**每一个**回声各自重演一次，
  发生在各自不同的时刻。最后一关 TWIN HOLDS 就靠这一点：全场只有一个哨兵、
  只能砍一刀，却需要把两道双联门分别撑开——`−3` 和 `−10` 两个回声，替你停了两次。

## 玩法

| 操作 | 按键 |
| --- | --- |
| 移动（每回合必须移动） | `↑ ↓ ← →` / `WASD` |
| 挥砍（仅在身旁有哨兵时可用） | `空格` / `E`，或点方向盘中央的 STRIKE |
| 撤销 | `Z` / `U` / `Backspace` |
| 重来本关 | `R` |
| 播放最优解 DEMO | `P`，或点 DEMO 按钮；再按一次停止 |
| 切换关卡 | `[` `]` 或点击关卡号 |

手机上滑动屏幕移动，**点一下屏幕是挥砍**（身旁没有哨兵时点了没反应——它不是「跳过回合」的后门）。

**看板**：实心暖色方块是「现在的你」；青/品红棋盘抖动的方块是「回声」，角标 `−3` 表示它落后你 3 回合；
四角标记是**回声下一步要去的格子**——所有悖论都可以提前看见，游戏不会阴你。
压力板的指示环画在格子边缘，所以就算有东西站在上面，你依然看得出它是否被踩住。
碎片是青绿色的宝石，哨兵是紫色带角的方块；侧栏的 STRIKE 按钮**只在身旁真有哨兵时才亮**，
出口在还有碎片没捡时是**暗的、带一道横杠**。

失败只有两种：**PARADOX**（回声撞上你）和 **STUCK**（四个方向全被堵死，而你又不能不动）。
两种都可以无限撤销。

## 关卡

二十三关，每一关只教一件事。前十三关只用墙 / 出口 / 压力板 / 闸门，后十关加入碎片与哨兵：

| # | 关卡 | 回声延迟 | 最优步数 | 教什么 |
| --- | --- | --- | --- | --- |
| 1 | STEP | −3 | 14 | 必须移动；认识回声 |
| 2 | BLINK | −3 | 8 | 门只闪一回合 |
| 3 | DETOUR | −4 | 10 | 不能等，就得绕——绕路永远消耗偶数回合 |
| 4 | DEAD END | −5 | 17 | 一格宽死路里必然撞上自己 |
| 5 | RING VAULT | −6 | 14 | 环形走廊，闪门时你该在哪 |
| 6 | TWO DOORS | −5 | 14 | 两道门 = 回板两次，间隔要正好 |
| 7 | TWO OF YOU | −3 / −6 | 13 | 两块板两个回声，同时踩住 |
| 8 | TRIO | −4 / −6 / −8 | 14 | 三块板三个回声，间隔两回合 |
| 9 | ASYMMETRY | −7 | 19 | 两个房间不等宽，两次闪门间隔不同 |
| 10 | THE LONG WAY | −2 / −7 | 18 | 大房间里，两个相隔五回合的回声要同时覆盖两块板 |
| 11 | DEEPER | −9 | 21 | 同样的房间，延迟九回合 |
| 12 | CLOCKWORK | −4 / −9 | 20 | 两板两回声 + 大房间，只有一种路径形状对得上 |
| 13 | THREE DOORS | −9 | 24 | 三道门一块板，必须先在板上踩满四次 |
| 14 | RELIC | −4 | 10 | **碎片**：出口封着，先去捡 |
| 15 | HOARD | −5 | 16 | 门两侧各一块碎片，而你只有一次闪门 |
| 16 | THE GUARD | −3 | 11 | **哨兵 / 挥砍**：门后被堵死，得站在门洞里砍 |
| 17 | OFF BEAT | −7 | 13 | 没人挡路，只是**差一步奇偶**——砍一刀补上 |
| 18 | HELD OPEN | −6 | 13 | 双联门：站在板上砍，让回声连踩两回合 |
| 19 | RELIQUARY | −7 | 17 | 碎片在门后，而门后站着哨兵 |
| 20 | THE LONG HELD | −8 | 19 | 双联门 + 门后的碎片 |
| 21 | VAULT RUN | −2 / −13 | 29 | 同一道门要穿两次，回程靠那个远回声 |
| 22 | DEEP VAULT | −9 | 20 | 两道串联门，碎片在最里面 |
| 23 | TWIN HOLDS | −3 / −10 | 25 | 两道双联门，只有一个哨兵——一刀，两个回声各停一拍 |

## 每关都有 DEMO

卡住了就按 **`P`**（或点侧栏的 DEMO 按钮），游戏会**逐步演示这一关的最优解**，
按钮上实时显示进度（`STOP 7/24`），播放期间你的输入会被锁住，再按一次随时停下。

这些演示不是我手写的提示，而是**求解器算出来的最优路线**：
`src/solutions.js` 由 `npm run solutions` 从 BFS 生成，`npm test` 会校验它仍然
最优且仍能通关——关卡一改，测试立刻报 `STALE` 并让你重新生成。

文字版答案在 [`docs/SOLUTIONS.md`](docs/SOLUTIONS.md)（剧透警告），同样是自动生成的。

## 关卡是被证明过的，不是试出来的

`tools/solver.mjs` 是一个跑在**与游戏完全相同的规则引擎**上的广度优先求解器。
`npm test` 对每一关断言五件事：

1. **可解** —— 存在一条通关路线；
2. **最优步数正确** —— 表格里的步数由 BFS 重新算出，改动关卡后如果最优解变了，CI 直接失败；
3. **离开机制就无解** —— 把回声全部移除后，该关必须**变得无解**；
4. **有哨兵的关卡，不许挥砍也必须无解** —— 把 `strike` 从动作集里删掉重搜一遍；
5. **DEMO 的解法没过期** —— 存档的最优解必须仍能通关、且步数仍等于最优。

第 3、4 条是最重要的：它们保证每一个机制都是**承重的**，而不是装饰。
任何一关如果不用回声也能过，或者哨兵只是一扇「换了皮的锁着的门」，测试就红。
写第 16 关时第一版就被第 4 条抓到过：哨兵堵在走廊上，但上下两行是通的，
玩家绕过去就行了——测试直接报 `solvable WITHOUT striking; the sentinel is decoration`。

最右边的 `states` 是求解器探索过的状态数，也是一个还算诚实的难度代理指标：
第 12 关要搜 47 万个状态才能确定最优解。

```
  #  level                    delay  optimal  echo-required  strike  demo   states   time
  -----------------------------------------------------------------------------------------
  1  STEP                     3      14       tutorial       -       ok     47       2ms
  2  BLINK                    3      8        yes            -       ok     210      4ms
  3  DETOUR                   4      10       yes            -       ok     448      8ms
  4  DEAD END                 5      17       yes            -       ok     187      2ms
  5  RING VAULT               6      14       yes            -       ok     481      3ms
  6  TWO DOORS                5      14       yes            -       ok     2335     18ms
  7  TWO OF YOU               3/6    13       yes            -       ok     10934    34ms
  8  TRIO                     4/6/8  14       yes            -       ok     20788    59ms
  9  ASYMMETRY                7      19       yes            -       ok     24997    70ms
  10 THE LONG WAY             2/7    18       yes            -       ok     14990    49ms
  11 DEEPER                   9      21       yes            -       ok     153543   848ms
  12 CLOCKWORK                4/9    20       yes            -       ok     478302   3397ms
  13 THREE DOORS              9      24       yes            -       ok     267332   1381ms
  14 RELIC                    4      10       yes            -       ok     856      2ms
  15 HOARD                    5      16       yes            -       ok     6503     28ms
  16 THE GUARD                3      11       yes            yes     ok     263      0ms
  17 OFF BEAT                 7      13       yes            yes     ok     17957    48ms
  18 HELD OPEN                6      13       yes            yes     ok     7390     20ms
  19 RELIQUARY                7      17       yes            yes     ok     17079    63ms
  20 THE LONG HELD            8      19       yes            yes     ok     68633    310ms
  21 VAULT RUN                2/13   29       yes            -       ok     31725    106ms
  22 DEEP VAULT               9      20       yes            -       ok     141504   1038ms
  23 TWIN HOLDS               3/10   25       yes            yes     ok     229766   1307ms
```

## 本地运行

无需安装任何依赖。

```bash
npm run dev        # http://localhost:5173
npm test           # 验证全部关卡（含 DEMO 解法是否过期）
npm run solutions  # 重新生成 src/solutions.js 和 docs/SOLUTIONS.md
npm run solve -- 13 # 打印第 13 关的最优解
```

## 自己加一关

在 `src/levels.js` 里加一个对象，然后 `npm test` 会告诉你它是否成立：

```js
{
  id: 14,
  title: 'YOUR LEVEL',
  delays: [5],        // 一个回声，落后 5 回合；写 [3, 6] 就是两个
  par: 0,             // 先随便填，测试会告诉你真实的最优步数
  hint: 'One line of guidance for the player.',
  grid: [
    '#########',
    '#@..#X..#',
    '#...g...#',
    '#p..#...#',
    '#########',
  ],
}
```

图例：`#` 墙 · `.` 地板 · `@` 你 · `X` 出口 · `p` 压力板 · `g` 闸门 · `*` 碎片 · `S` 哨兵。
闸门在**所有**压力板都被实体踩住时开启——所以 N 块板意味着你需要 N 个身体。
出口在 `*` 全部捡完之前是封死的；`S` 挡住它那一格，直到你在旁边挥砍。
每关最多 16 个碎片、16 个哨兵（它们各占状态键里的一个字符）。

加完关卡跑一次 `npm run solutions`，新关的 DEMO 和答案文档会自动补上。

设计关卡时记住那条奇偶律：**板到门口的距离和延迟必须同奇偶**，否则无论怎么走都不可能成立——
除非你在关里放一个哨兵，那玩家就能砍一刀把奇偶掰回来（第 17 关就是整关围绕这一点造的）。

`tools/probe.mjs` 可以批量试关，直接告诉你一批候选关卡各自是否可解、最优几步、搜索空间多大。
实际造关时最省事的办法是**固定一张图，把 `delays` 扫一遍**：可解的延迟往往只占一半（另一半被奇偶律毙掉），
而搜索状态数会随延迟指数上涨，等于免费给你一条难度旋钮。上面第 14–23 关就是这么扫出来的。

## 设计笔记

几个刻意的取舍：

- **去掉"等待"是整个设计的转折点。** 有等待时，机制是"你在板上站多久，门就开多久"——
  好懂，但也太软：多站一回合就能救回一个没算准的时机。去掉之后，门变成只闪一回合，
  所有容错消失，玩家被迫**真的去算**回合数和奇偶性。同一批关卡的搜索空间因此涨了一个数量级。
- **回合制而非实时**。解谜的乐趣在于"想明白"，不在于"手速"。回合制还带来完全的确定性，
  这正是求解器能证明关卡的前提。
- **无限撤销**。悖论和失误的惩罚应该是零，玩家才敢去试探规则的边界。
- **前十三关只有 4 种元素**（墙、出口、压力板、闸门）。难度全部来自 `延迟`、`距离`、`门的数量`
  这三个数字的相互作用，而不是来自不断堆新机制。第 13 关和第 2 关用的是同一套东西。
- **后来加的两样，标准是「必须能打破一条既有的硬约束」**，而不是「再多一种收集品」。
  碎片打破的是「路线 = 起点到终点」；挥砍打破的是「不许原地不动」，
  而后者一破，奇偶律和「门只闪一下」这两条推论**同时**松动——一个动作，两条老规则被撬开。
  这也是为什么 `npm test` 里专门加了第 4 条断言：新机制如果不能让某关**从无解变有解**，
  它就没有存在的理由。
- **挥砍只有一个键，没有方向。** 一刀清一圈，是为了让操作停在「四个方向 + 一个动作」，
  代价是同一格只值一次暂停——这不是漏洞，是关卡设计里明摆着的一条预算。
- **闸门规则是全局的**（所有板 → 所有门），而不是一板一门。少一条规则，多一层组合。
- **回声不会挡住你的视线**：它下一步的位置永远画在屏幕上。信息完全公开，难的是推理，不是猜。

## 技术

原生 JavaScript + Canvas，ES Modules，零运行时依赖，零构建步骤。
`src/engine.js` 是纯函数、不碰 DOM——浏览器和求解器跑的是同一份代码。

画面是真像素风：每个格子按 8×8 精灵网格绘制，格子尺寸强制为 8 的倍数，
所有坐标取整并关闭抗锯齿，连数字都是代码里自绘的 3×5 位图字。
回声用青/品红**互补相位的棋盘抖动**叠出来——同一个身体，被拆成两个错开的色彩通道。

界面文字为英文。标题字体用 Google Fonts 的 Silkscreen，加载失败会回退到系统等宽字体，
不影响任何功能。

## License

MIT
